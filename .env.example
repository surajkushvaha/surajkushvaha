# Copy to .env and fill in. .env is gitignored and must stay that way.
#
# Ollama Cloud key, used only by the serverless function in api/ask.ts.
# It is deliberately NOT prefixed with VITE_: anything VITE_-prefixed is
# inlined into the client bundle and would be public.
OLLAMA_API_KEY=
